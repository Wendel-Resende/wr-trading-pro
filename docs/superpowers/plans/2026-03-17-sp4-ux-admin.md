# Sub-projeto 4 — UX & Admin: Implementação

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Melhorar UX e visibilidade operacional: injetar contexto de mercado no AI Chat, substituir o AdminTab stub por um painel de saúde real dos serviços Python, e adicionar debounce nos updates de tick para reduzir re-renders excessivos.

**Architecture:** `llmService.ts` recebe um helper `buildMarketContext()` que empacota dados live do MT5 e injeta no system prompt antes de cada requisição LLM. O `AdminTab` faz polling a uma nova rota `/api/admin/services-status` (health checks server-side com timeout 2s) e complementa com o estado do mt5_bridge via `mt5Service.getConnectionState()` client-side. O `page.tsx` acumula ticks num `useRef` e faz flush para o estado React com debounce de 200ms.

**Tech Stack:** Next.js 15, React 19, TypeScript, Tailwind CSS (cyber-card/neon classes), native `fetch` com `AbortController`, `useRef` + `setTimeout` para debounce

---

## Mapa de Arquivos

### Criados
- `src/app/api/admin/services-status/route.ts` — health checks server-side (spread_api, volatility_api)

### Modificados
- `src/services/llmService.ts` — adicionar `MarketContext`, `buildMarketContext()`, estender `buildSystemMessage()`
- `src/components/tabs/AdminTab.tsx` — reescrever com cards reais de status + latência + auto-refresh
- `src/app/page.tsx` — adicionar debounce 200ms no handleTick

---

## Task 1: AI Chat com Contexto de Mercado

**Files:**
- Modify: `src/services/llmService.ts`

- [ ] **Step 1: Ler llmService.ts para entender estrutura atual**

```bash
cat "C:\Users\rwres\OneDrive\Área de Trabalho\AI\wr_trade_pro_\src\services\llmService.ts"
```

Identificar: onde está `buildSystemMessage()`, que campos o `LLMChatRequest` aceita em `context`, e onde `chat()` chama `buildSystemMessage()`.

- [ ] **Step 2: Adicionar interface MarketContext e helper buildMarketContext()**

No arquivo `src/services/llmService.ts`, após os imports e antes da definição da classe, adicionar:

```typescript
// ─── Market Context ──────────────────────────────────────────────────────────

export interface MarketContext {
  symbol: string;
  bid: number;
  ask: number;
  spread: number;
  accountBalance: number;
  accountEquity: number;
  dailyResult: number;
  openPositions: Array<{
    ticket: number;
    symbol: string;
    type: string;
    volume: number;
    openPrice: number;
    currentPrice: number;
    profit: number;
  }>;
  timestamp: string;
}

/**
 * Build a MarketContext object from live MT5 data.
 * Call this immediately before llmService.chat() in the UI layer.
 */
export function buildMarketContext(
  accountInfo: { balance: number; equity: number; profit: number } | null,
  tickData: Map<string, { bid?: number; ask?: number }>,
  selectedSymbol: string
): MarketContext | null {
  if (!accountInfo) return null;

  const tick = tickData.get(selectedSymbol);
  const bid = tick?.bid ?? 0;
  const ask = tick?.ask ?? 0;

  return {
    symbol: selectedSymbol,
    bid,
    ask,
    spread: parseFloat((ask - bid).toFixed(5)),
    accountBalance: accountInfo.balance,
    accountEquity: accountInfo.equity,
    dailyResult: accountInfo.profit,
    openPositions: [],
    timestamp: new Date().toISOString(),
  };
}
```

- [ ] **Step 3: Estender buildSystemMessage() para renderizar MarketContext**

Localizar o método `private buildSystemMessage(context: any): LLMMessage` e substituir seu body por:

```typescript
  private buildSystemMessage(context: any): LLMMessage {
    let contextText = 'You are a professional trading assistant for the WR Trading Pro platform. ';
    contextText += 'Answer in the same language the user writes in. ';

    // Structured market context (from buildMarketContext helper)
    if (context?.market) {
      const m = context.market as MarketContext;
      contextText +=
        `\n\n[LIVE MARKET CONTEXT — ${m.timestamp}]\n` +
        `Symbol: ${m.symbol} | Bid: ${m.bid} | Ask: ${m.ask} | Spread: ${m.spread}\n` +
        `Balance: ${m.accountBalance} | Equity: ${m.accountEquity} | Daily P&L: ${m.dailyResult}\n`;

      if (m.openPositions.length > 0) {
        contextText += `Open Positions (${m.openPositions.length}):\n`;
        m.openPositions.forEach((p) => {
          contextText +=
            `  ${p.type} ${p.volume} ${p.symbol} @ ${p.openPrice} (now ${p.currentPrice}, P&L ${p.profit})\n`;
        });
      } else {
        contextText += `Open Positions: none\n`;
      }
    }

    // Legacy generic fields (backward compat)
    if (context?.marketData) {
      contextText += `\nMarket data: ${JSON.stringify(context.marketData)}. `;
    }
    if (context?.portfolio) {
      contextText += `Portfolio: ${JSON.stringify(context.portfolio)}. `;
    }
    if (context?.indicators) {
      contextText += `Indicators: ${JSON.stringify(context.indicators)}. `;
    }

    contextText += '\nProvide clear, actionable trading advice with proper risk management.';

    return { role: 'system', content: contextText };
  }
```

- [ ] **Step 4: Ler AIChat.tsx para entender props atuais e como chat() é chamado**

```bash
cat "C:\Users\rwres\OneDrive\Área de Trabalho\AI\wr_trade_pro_\src\components\AIChat.tsx"
```

Verificar: (a) quais props o componente recebe, (b) como `llmService.chat()` é invocado, (c) se `accountInfo` e `tickData` já chegam como props.

- [ ] **Step 5: Atualizar o call site do llmService no AIChat (ou ChatTab)**

Ler o componente de chat para verificar como `llmService.chat()` é chamado:

```bash
grep -rn "llmService.chat\|buildMarketContext\|context.*market" "C:\Users\rwres\OneDrive\Área de Trabalho\AI\wr_trade_pro_\src\components" --include="*.tsx" --include="*.ts"
```

No componente de chat, adicionar:

```typescript
import { llmService, buildMarketContext } from '@/services/llmService';

// Dentro do handler de envio de mensagem:
const marketCtx = buildMarketContext(accountInfo, tickData, selectedSymbol);

const response = await llmService.chat({
  messages: chatHistory,
  config: { provider: selectedProvider },
  context: marketCtx ? { market: marketCtx } : undefined,
});
```

Se `AIChat.tsx` não recebe `accountInfo` e `tickData` como props, adicioná-los como props opcionais ou usar `MT5ServiceSingleton` para ler o estado diretamente.

- [ ] **Step 5: Verificar TypeScript**

```bash
cd "C:\Users\rwres\OneDrive\Área de Trabalho\AI\wr_trade_pro_"
npx tsc --noEmit 2>&1 | head -30
```

- [ ] **Step 6: Commit**

```bash
cd "C:\Users\rwres\OneDrive\Área de Trabalho\AI\wr_trade_pro_"
git add src/services/llmService.ts src/components/
git commit -m "feat(llm): buildMarketContext helper + injetar contexto de mercado no system prompt"
```

---

## Task 2: Admin com Métricas Reais

**Files:**
- Create: `src/app/api/admin/services-status/route.ts`
- Modify: `src/components/tabs/AdminTab.tsx`

- [ ] **Step 1: Verificar API do mt5Service (getConnectionState + eventos)**

```bash
grep -n "getConnectionState\|\.on\b\|\.off\b\|emit\|EventEmitter" "C:\Users\rwres\OneDrive\Área de Trabalho\AI\wr_trade_pro_\src\services\mt5Service.ts" | head -20
```

Confirmar que `mt5Service` expõe `.on(event, handler)` e `.off(event, handler)` (padrão EventEmitter) e que `getConnectionState()` retorna `{ state: string }`.

- [ ] **Step 2: Criar a pasta e o API route**

```bash
mkdir -p "C:\Users\rwres\OneDrive\Área de Trabalho\AI\wr_trade_pro_\src\app\api\admin\services-status"
```

Criar `src/app/api/admin/services-status/route.ts`:

```typescript
import { NextResponse } from 'next/server';

export const dynamic = 'force-dynamic';

interface ServiceResult {
  name: string;
  url: string;
  status: 'online' | 'offline';
  latencyMs: number | null;
  error: string | null;
}

async function checkHttp(name: string, url: string, timeoutMs = 2000): Promise<ServiceResult> {
  const controller = new AbortController();
  const timer = setTimeout(() => controller.abort(), timeoutMs);
  const start = Date.now();

  try {
    const res = await fetch(url, { signal: controller.signal, cache: 'no-store' });
    clearTimeout(timer);
    const latencyMs = Date.now() - start;
    // Qualquer resposta < 500 indica serviço ativo (mesmo 404 se não tiver /health)
    if (res.status < 500) {
      return { name, url, status: 'online', latencyMs, error: null };
    }
    return { name, url, status: 'offline', latencyMs, error: `HTTP ${res.status}` };
  } catch (err: unknown) {
    clearTimeout(timer);
    const latencyMs = Date.now() - start;
    const msg = err instanceof Error ? err.message : 'connection refused';
    return { name, url, status: 'offline', latencyMs, error: msg };
  }
}

export async function GET() {
  const checks = await Promise.all([
    checkHttp('spread_api', 'http://localhost:5000/health'),
    checkHttp('volatility_api', 'http://localhost:5555/health'),
  ]);

  return NextResponse.json({
    services: checks,
    checkedAt: new Date().toISOString(),
  });
}
```

- [ ] **Step 2: Reescrever AdminTab.tsx**

```typescript
"use client";

import { useState, useEffect, useCallback } from "react";
import { RefreshCw, CheckCircle, XCircle, Loader2, Wifi, WifiOff } from "lucide-react";
import { MT5ServiceSingleton } from "@/services/mt5Service";

interface ServiceResult {
  name: string;
  url: string;
  status: "online" | "offline";
  latencyMs: number | null;
  error: string | null;
}

interface ServicesPayload {
  services: ServiceResult[];
  checkedAt: string;
}

const mt5Service = MT5ServiceSingleton.getInstance();

const SERVICE_LABELS: Record<string, string> = {
  spread_api: "Spread API (B3)",
  volatility_api: "Volatility API",
  mt5_bridge: "MT5 Bridge (WebSocket)",
};

export default function AdminTab() {
  const [data, setData] = useState<ServicesPayload | null>(null);
  const [mt5Status, setMt5Status] = useState<"online" | "offline">("offline");
  const [loading, setLoading] = useState(false);
  const [lastRefresh, setLastRefresh] = useState<Date | null>(null);

  const fetchStatus = useCallback(async () => {
    setLoading(true);
    try {
      const res = await fetch("/api/admin/services-status", { cache: "no-store" });
      if (res.ok) {
        const json: ServicesPayload = await res.json();
        setData(json);
      }
    } catch (err) {
      console.error("Admin status fetch failed:", err);
    } finally {
      setLoading(false);
      setLastRefresh(new Date());
    }
  }, []);

  useEffect(() => {
    const syncMt5 = () => {
      const state = mt5Service.getConnectionState();
      setMt5Status(state.state === "CONNECTED" ? "online" : "offline");
    };
    syncMt5();
    mt5Service.on("state", syncMt5);
    return () => { mt5Service.off("state", syncMt5); };
  }, []);

  useEffect(() => {
    fetchStatus();
    const interval = setInterval(fetchStatus, 30_000);
    return () => clearInterval(interval);
  }, [fetchStatus]);

  const allServices: ServiceResult[] = [
    ...(data?.services ?? []),
    {
      name: "mt5_bridge",
      url: "ws://localhost:8766 (mt5Service)",
      status: mt5Status,
      latencyMs: null,
      error: mt5Status === "offline" ? "Not connected" : null,
    },
  ];

  const onlineCount = allServices.filter((s) => s.status === "online").length;

  return (
    <div className="cyber-card p-6 hud-corner">
      <div className="flex items-center justify-between mb-6">
        <div>
          <h2 className="font-orbitron text-2xl font-bold text-white neon-text-cyan">
            Administração
          </h2>
          <p className="text-xs text-gray-400 font-space mt-1">
            {onlineCount}/{allServices.length} serviços online
            {lastRefresh && (
              <span className="ml-2 text-gray-600">
                — {lastRefresh.toLocaleTimeString("pt-BR")}
              </span>
            )}
          </p>
        </div>
        <button
          onClick={fetchStatus}
          disabled={loading}
          className="cyber-button cyber-button-secondary flex items-center gap-2"
        >
          {loading
            ? <Loader2 className="w-4 h-4 animate-spin" />
            : <RefreshCw className="w-4 h-4" />}
          <span className="font-space text-sm">Atualizar</span>
        </button>
      </div>

      <div className="grid grid-cols-1 md:grid-cols-3 gap-4 mb-6">
        {allServices.map((svc) => (
          <ServiceCard key={svc.name} service={svc} loading={loading && !data} />
        ))}
      </div>

      <div className="text-xs text-gray-500 font-space border-t border-cyber-border pt-4">
        Auto-refresh a cada 30s. Timeout HTTP: 2s. mt5_bridge via WebSocket singleton.
      </div>
    </div>
  );
}

function ServiceCard({
  service,
  loading,
}: {
  service: ServiceResult;
  loading: boolean;
}) {
  const isOnline = service.status === "online";
  const label = SERVICE_LABELS[service.name] ?? service.name;

  return (
    <div
      className={`cyber-card p-4 hud-corner border transition-colors ${
        loading ? "border-gray-700" : isOnline ? "border-green-500/40" : "border-red-500/40"
      }`}
    >
      <div className="flex items-start justify-between mb-3">
        <h3 className="font-orbitron text-sm text-white leading-tight">{label}</h3>
        {loading ? (
          <Loader2 className="w-4 h-4 text-gray-500 animate-spin flex-shrink-0" />
        ) : isOnline ? (
          <CheckCircle className="w-4 h-4 text-green-400 flex-shrink-0" />
        ) : (
          <XCircle className="w-4 h-4 text-red-400 flex-shrink-0" />
        )}
      </div>

      <div className="flex items-center gap-2 mb-2">
        {isOnline
          ? <Wifi className="w-3 h-3 text-green-400" />
          : <WifiOff className="w-3 h-3 text-red-400" />}
        <span className={`text-sm font-space font-semibold ${isOnline ? "text-green-400" : "text-red-400"}`}>
          {loading ? "verificando..." : isOnline ? "ONLINE" : "OFFLINE"}
        </span>
      </div>

      {service.latencyMs !== null && (
        <p className="text-xs text-gray-400 font-jetbrains">
          Latência:{" "}
          <span className={
            service.latencyMs < 100 ? "text-green-400"
            : service.latencyMs < 500 ? "text-yellow-400"
            : "text-red-400"
          }>
            {service.latencyMs} ms
          </span>
        </p>
      )}

      {service.error && (
        <p className="text-xs text-red-400/70 font-space mt-1 truncate" title={service.error}>
          {service.error}
        </p>
      )}

      <p className="text-xs text-gray-600 font-space mt-2 truncate" title={service.url}>
        {service.url}
      </p>
    </div>
  );
}
```

- [ ] **Step 3: Verificar TypeScript**

```bash
cd "C:\Users\rwres\OneDrive\Área de Trabalho\AI\wr_trade_pro_"
npx tsc --noEmit 2>&1 | head -30
```

- [ ] **Step 4: Commit**

```bash
git add src/components/tabs/AdminTab.tsx src/app/api/admin/
git commit -m "feat(admin): painel real de status dos serviços com health check e latência"
```

---

## Task 3: Performance — Debounce tick re-renders em page.tsx

**Files:**
- Modify: `src/app/page.tsx`

- [ ] **Step 1: Ler page.tsx para localizar handleTick e o useEffect de MT5**

```bash
grep -n "handleTick\|setTickData\|useRef\|import.*React" "C:\Users\rwres\OneDrive\Área de Trabalho\AI\wr_trade_pro_\src\app\page.tsx" | head -20
```

- [ ] **Step 2: Adicionar useRef ao import do React se não estiver**

Localizar a linha de import do React e garantir que `useRef` está incluído:

```typescript
import { useState, useEffect, useRef, lazy, Suspense } from "react";
```

- [ ] **Step 3: Adicionar refs de debounce dentro do componente**

Após as declarações de estado existentes, adicionar:

```typescript
  const pendingTicksRef = useRef<Map<string, MT5Tick>>(new Map());
  const tickDebounceRef = useRef<ReturnType<typeof setTimeout> | null>(null);
```

- [ ] **Step 4: Substituir handleTick pela versão com debounce**

Localizar o `handleTick` atual (algo como):
```typescript
    const handleTick = (tick: MT5Tick) => {
      if (tick.symbol && tick.bid) {
        setTickData((prev) => new Map(prev).set(tick.symbol!, tick));
      }
    };
```

Substituir por:
```typescript
    const handleTick = (tick: MT5Tick) => {
      if (!tick.symbol || !tick.bid) return;

      pendingTicksRef.current.set(tick.symbol, tick);

      if (tickDebounceRef.current) {
        clearTimeout(tickDebounceRef.current);
      }

      tickDebounceRef.current = setTimeout(() => {
        const pending = pendingTicksRef.current;
        if (pending.size === 0) return;
        setTickData((prev) => {
          const next = new Map(prev);
          pending.forEach((t, sym) => next.set(sym, t));
          return next;
        });
        pendingTicksRef.current = new Map();
        tickDebounceRef.current = null;
      }, 200);
    };
```

- [ ] **Step 5: Adicionar clearTimeout ao cleanup do useEffect**

Na função de cleanup do useEffect de MT5, adicionar:

```typescript
      if (tickDebounceRef.current) {
        clearTimeout(tickDebounceRef.current);
      }
```

- [ ] **Step 6: Verificar TypeScript**

```bash
cd "C:\Users\rwres\OneDrive\Área de Trabalho\AI\wr_trade_pro_"
npx tsc --noEmit 2>&1 | head -20
```

- [ ] **Step 7: Commit**

```bash
git add src/app/page.tsx
git commit -m "perf(shell): debounce 200ms nos updates de tick para reduzir re-renders"
```

---

## Validação Final do Sub-projeto 4

- [ ] `npm run dev` inicia sem erros
- [ ] AI Chat: com MT5 conectado e símbolo selecionado, o system prompt inclui preço atual e saldo (verificar no network tab do browser)
- [ ] Admin: aba mostra 3 cards com status real dos serviços
- [ ] Admin: clicar em "Atualizar" refresca os status
- [ ] Admin: mt5_bridge card mostra ONLINE quando MT5 está conectado
- [ ] Dashboard: com ticks chegando, a UI atualiza suavemente (não trava)
- [ ] TypeScript: `npx tsc --noEmit` sem erros

```bash
cd "C:\Users\rwres\OneDrive\Área de Trabalho\AI\wr_trade_pro_"
npx tsc --noEmit 2>&1 | wc -l  # deve ser 0
git log --oneline | head -5
```
